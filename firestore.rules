rules_version = '2';

// All access goes through Cloud Functions using the Admin SDK.
// Direct client access is denied for all collections.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
